import { Card, CardProps } from "antd";
import React from "react";
import styled from "styled-components";

export const LeftBarCard: React.FunctionComponent<CardProps> = styled(Card)`
  width: 300;
  margin-top: 24px;
`;
